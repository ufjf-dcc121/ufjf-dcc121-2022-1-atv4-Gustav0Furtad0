//função exibi a localização dos principais elementos
function margens() {
    console.log(fazendeiro.getBoundingClientRect());
    console.log(maresquerda.getBoundingClientRect());
    console.log(mardireita.getBoundingClientRect());
}